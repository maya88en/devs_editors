// "use client";

// import React, { useEffect, useState } from "react";
// import { usePathname } from "next/navigation";
// import { RoomCard, RoomCardSkeleton } from "../dashboard/RoomCard";
// import { fetchDrawingRoomById } from "@/app/services/drawing-room.service";
// import { RoomType } from "../dashboard/DashboardBody";
// import { Props } from "next/script";




 
  // const createAt = new Date(created_at);
  // const [roomName, setName] = useState<RoomType[] | null>(null);
  // const [loading, setLoading] = useState<boolean>(true);
  // const { session } = props;
  // const pathname = usePathname();
  // const isDashboard = pathname === "/";

  
  // const [loading, setLoading] = useState<boolean>(true);
  // const [RoomId, setRoomId] = useState<boolean>(false);
  

  // const loadRoom = async () => {
  //     return fetchDrawingRoomById(id).then((res) => {
  //       setName(res);
  //     });
  //   };

// export type RoomType = {
//   id: string;
//   name: string;
//   created_at: string;
//   isPublic: boolean;
// };

// type Props = {
//   session: any;
// };

// const Hello = (props: Props) => {
  

  // Conditions
  // const hasNotCreatedARoom = !loading && rooms?.length === 0;
  // const hasAtLeastOneRoom = rooms && rooms!.length >= 0;
  // const shouldShowRoom = !loading && hasAtLeastOneRoom;
    
  
    
  // interface railsRoomProps {
  //   isRails: boolean;
  //   id: string;
    
  // }  
    
  // export const Dash = async ({ id, name, created_at, isPublic }: RoomType) => {
  //   const [rooms, setRooms] = useState<RoomType[] | []>([]);
     
  //     await fetchDrawingRoomById(id);
  //   if (id) 
  //     (
  //       <RoomCard
  //         key={id}
  //         id={"0b59fd60-273d-489e-b944-cb4226b23953"}
  //         name={name}
  //         created_at={created_at}
  //         isPublic={isPublic} />

  //     );
  //     return (<p>Welcome to rails room</p>);}
      
      



  







